---
name: Bug report
description: Something verifies wrong, crashes, or disagrees between implementations
labels: [bug]
---

## What happened

<!-- One paragraph: expected result vs actual result. -->

## Reproduce

<!-- Minimal commands or code. Prefer fixture domains (example.com) over real sites. -->

```bash
node bin/cli.js validate example.com --json
```

## Environment

- AIFeed version / commit:
- Node (`node --version`) or Python (`python --version`):
- OS:

## Evidence

<!-- Paste `errors[].code` values or the failing assertion. Never paste private keys. -->
